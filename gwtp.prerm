#! /bin/sh
# postinst script for gwtp
#
# see: dh_installdeb(1)

# Automatically added by dh_installdocs
if ( test "$1" = "upgrade" || test "$1" = "remove" ) && test -L /usr/doc/gwtp ; then
	rm -f /usr/doc/gwtp
fi
# End automatically added section

#DEBHELPER#
